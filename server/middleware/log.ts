export default defineEventHandler(event => {
    // const message = 'New request: ' + getRequestURL(event);
    // console.log('New request:', message); // Відобразиться в терміналі
    // return { message, event }; // Відобразиться в браузері
});
