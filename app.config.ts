//  app.config: використовується для надання загальнодоступних змінних, які можна визначити під час збирання, конфігурація веб-сайту, наприклад варіант теми, назва та будь-яка конфігурація проекту, які не є конфіденційними.

export default defineAppConfig({
    title: 'Hello Nuxt',
    theme: {
        dark: true,
        colors: {
            primary: '#ff0000',
        },
    },
});
